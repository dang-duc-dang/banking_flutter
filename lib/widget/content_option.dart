import 'package:flutter/cupertino.dart';

class ContentOptions extends StatelessWidget {
  final String type;

  ContentOptions({required this.type});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
